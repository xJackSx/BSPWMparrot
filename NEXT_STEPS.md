To push your changes to the remote repository `git@github.com:gabykap29/BSPWMparrotUBUNTU.git`, please run the following command from within the `BSPWMparrot` directory:

```bash
git push -u origin master
```

**Note**: The `README_Ubuntu_Installation.md` file is now redundant as its content has been merged into `README.md`. You can delete it locally if you wish:

```bash
rm README_Ubuntu_Installation.md
```

All requested tasks have been completed.